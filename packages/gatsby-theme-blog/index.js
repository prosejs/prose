const { configureComponents } = require('@prose/gatsby-theme/theme/configure')
const { toSourceUri } = require('@prose/gatsby-theme/core/to-source-uri')

module.exports = {
  configureComponents,
  toSourceUri,
}
